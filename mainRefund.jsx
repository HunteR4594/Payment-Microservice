import React from "react";
import ReactDOM from "react-dom/client";
import AppRefund from "./AppRefund.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AppRefund />
  </React.StrictMode>
);
